{% extends 'base.html.twig' %}

{% block title %}Lord{% endblock %}

{% block body %}
    <h1>Lord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lord.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lord.name }}</td>
            </tr>
            <tr>
                <th>Portrait</th>
                <td>{{ lord.portrait }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lord_index') }}">back to list</a>

    <a href="{{ path('lord_edit', {'id': lord.id}) }}">edit</a>

    {{ include('lord/_delete_form.html.twig') }}
{% endblock %}
