{% extends 'base.html.twig' %}

{% block title %}EffectType{% endblock %}

{% block body %}
    <h1>EffectType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ effect_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ effect_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('effect_type_index') }}">back to list</a>

    <a href="{{ path('effect_type_edit', {'id': effect_type.id}) }}">edit</a>

    {{ include('effect_type/_delete_form.html.twig') }}
{% endblock %}
