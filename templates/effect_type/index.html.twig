{% extends 'base.html.twig' %}

{% block title %}EffectType index{% endblock %}

{% block body %}
    <h1>EffectType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for effect_type in effect_types %}
            <tr>
                <td>{{ effect_type.id }}</td>
                <td>{{ effect_type.name }}</td>
                <td>
                    <a href="{{ path('effect_type_show', {'id': effect_type.id}) }}">show</a>
                    <a href="{{ path('effect_type_edit', {'id': effect_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('effect_type_new') }}">Create new</a>
{% endblock %}
