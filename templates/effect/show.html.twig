{% extends 'base.html.twig' %}

{% block title %}Effect{% endblock %}

{% block body %}
    <h1>Effect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ effect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ effect.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ effect.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ effect.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('effect_index') }}">back to list</a>

    <a href="{{ path('effect_edit', {'id': effect.id}) }}">edit</a>

    {{ include('effect/_delete_form.html.twig') }}
{% endblock %}
