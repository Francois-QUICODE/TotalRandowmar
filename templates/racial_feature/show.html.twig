{% extends 'base.html.twig' %}

{% block title %}RacialFeature{% endblock %}

{% block body %}
    <h1>RacialFeature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ racial_feature.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ racial_feature.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ racial_feature.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ racial_feature.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('racial_feature_index') }}">back to list</a>

    <a href="{{ path('racial_feature_edit', {'id': racial_feature.id}) }}">edit</a>

    {{ include('racial_feature/_delete_form.html.twig') }}
{% endblock %}
