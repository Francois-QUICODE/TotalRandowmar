<form method="post" action="{{ path('racial_feature_delete', {'id': racial_feature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ racial_feature.id) }}">
    <button class="btn">Delete</button>
</form>
